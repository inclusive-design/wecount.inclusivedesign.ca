name: Test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: npm install and lint
      env:
        CI: true
      run: |
        npm ci
        npm run lint
