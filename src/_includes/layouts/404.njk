{% extends 'layouts/base.njk' %}

{% block content %}
<article class="page">
    <h1>{{ title }}</h1>
    <div class="api-content">
        {{ content | safe }}
    </div>
</article>
{% endblock %}
{{ content | safe }}
